/**
 * @format
 */

import {AppRegistry} from 'react-native';
import ListaDatos from './src/containers/listaDatosContainer'
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () =>ListaDatos);
